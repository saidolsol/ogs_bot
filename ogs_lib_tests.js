var ogs_lib = require('./ogs_lib');

// ogs_lib.isMessageSentToday(function(err, isSent) {

//     if(isSent) {
//         console.log('Message already sent');
//     } else {
//         console.log('Need to send another message');
//     }

// });

// ogs_lib.messageSent(function(err) {

//     console.log(err);

// });